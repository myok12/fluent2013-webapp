define(function (require, exports, module) {
    "use strict";

    // a model for a preview of an article on the section page
    var ArticlePreview = Backbone.Model.extend({
    });

    module.exports = ArticlePreview;
});
