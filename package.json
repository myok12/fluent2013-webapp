{
    "name": "fluent2013-webapp",
    "version": "0.0.1",
    "scripts": {
        "start": "cd api && npm start",
        "test-frontend": "./node_modules/mocha/bin/mocha --reporter list --recursive tests/*"
    }
}
