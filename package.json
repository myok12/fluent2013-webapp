{
    "name": "fluent2013-webapp",
    "version": "0.0.1",
    "scripts": {
        "start": "cd api && npm start",
        "unit-tests": "./node_modules/mocha/bin/mocha --reporter list --recursive tests/unit/*",
        "integration-tests": "./run_tests.sh",
        "integration-tests-internal": "./node_modules/mocha/bin/mocha --reporter list --recursive tests/integration/*"
    }
}
